import 'package:flutter/material.dart';

class HorizontalGap extends SizedBox {
  const HorizontalGap({super.key, super.width = 8.0});
}

class VerticalGap extends SizedBox {
  const VerticalGap({super.key, super.height = 8.0});
}
